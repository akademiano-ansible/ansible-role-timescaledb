---
# defaults file for egeneralov.timescaledb

pgdg_version: 11

pgdg_shared_preload_libraries:
  - pg_stat_statements
  - pg_stat_kcache
  - timescaledb

tdb_postgresql_conf:
  - { "k": "timescaledb.telemetry_level", "v": "off" }

